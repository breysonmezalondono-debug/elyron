import { Layout } from '../components/Layout';

/**
 * Portal Campus — orientado al consumo/aprendizaje.
 * Reutiliza el shell compartido con el tutor IA activo (gamificación).
 */
export const CampusLayout = () => <Layout portal="campus" />;
