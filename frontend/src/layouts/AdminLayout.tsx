import { Layout } from '../components/Layout';

/**
 * Portal Staff / Admin — orientado a la producción y control.
 * Reutiliza el shell compartido con una vista más densa (sin tutor IA).
 */
export const AdminLayout = () => <Layout portal="admin" />;
